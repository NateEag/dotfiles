// Generated by CoffeeScript 1.6.3
var cli;

cli = require('../cli');

module.exports = function(program) {
  return program.command('close [names]').description('close all running IE processes in virtual machines').action(function(names, command) {
    return cli.fail(cli.find(names, 'running').found().all(function(vm) {
      return vm.close();
    }));
  });
};
