// Generated by CoffeeScript 1.6.3
var cli;

cli = require('../cli');

module.exports = function(program) {
  return program.command('stop [names]').description('stop virtual machines').option('-S, --no-save', 'power off the virtual machine').action(function(names, command) {
    return cli.fail(cli.find(names, 'running').found().all(function(vm) {
      return vm.stop(command.save);
    }));
  });
};
