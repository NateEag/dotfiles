// Generated by CoffeeScript 1.6.3
var cli;

cli = require('../cli');

module.exports = function(program) {
  return program.command('start [names]').description('start virtual machines').option('-h, --headless', 'start in headless (non-gui) mode').action(function(names, command) {
    return cli.fail(cli.find(names, '!missing', '!running').found().all(function(vm) {
      return vm.start(command.headless);
    }));
  });
};
