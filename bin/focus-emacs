#! /usr/bin/osascript

-- Bring Emacs to the foreground.
--
-- Sometimes I want to do that from bash scripts. This makes it possible on OS
-- X (though it's noticeably slow, which bugs me).
--
-- TODO Try using the Hammerspoon `hs` tool for this to see if it's faster.

tell application "Emacs"
    activate
end tell
